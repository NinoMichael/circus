export interface LoginData {
    email: string;
    password: string;
}

export interface RegisterData {
    email: string;
    role: string;
    contact: string;
    password: string;
    password_confirmation: string;
}